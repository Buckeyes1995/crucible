# Test plan — 2026-04-19 + 2026-04-20

Covers features added on 2026-04-19 (yesterday) that weren't verified and
everything shipped on 2026-04-20 (today).

**Prep:** check out `overnight/2026-04-20`, rebuild frontend, restart backend.

```bash
git checkout overnight/2026-04-20
cd backend && .venv/bin/uvicorn main:app --port 7777 --host 0.0.0.0 &
cd ../frontend && pnpm build && pnpm start &
open http://localhost:3000
```

Legend: **[smoke]** = 10s, **[standard]** = 1–5m, **[long]** = >5m / needs a live model.

---

## Yesterday (2026-04-19) — not yet verified

### Y1. Store: Propose entry
- **[smoke]** On /store, click Propose entry dropdown. Each of the 5 kinds opens a GitHub issue form.

### Y2. Store: Installed tab
- **[smoke]** /store → Installed. Sections show MCPs / Prompts / Workflows / System prompts / Models you've installed.
- **[standard]** Click Uninstall / Delete on a test entry. Verify it disappears after the toast.

### Y3. Store: MCP host — tools tester
- **[standard]** Expand Tools on the filesystem MCP card. Verify it spawns (first call takes ~5s), lists tools, and `list_directory` returns actual files when you give it a path.

### Y4. Store: HF search in Models tab
- **[smoke]** `/store` → Models → type "qwen". MLX toggle filters results; each result shows downloads/likes.
- **[standard]** Click Download on a small model. Active-downloads strip appears above results; status flips to ✓ Downloaded once complete.

### Y5. Profiler tok/s format
- **[smoke]** `/profiler` after at least one profiled request — table cells and bar-chart ticks show one decimal place, not 10+.

### Y6. Chat slash commands
- **[smoke]** `/help`, `/temp 0.3`, `/max 1024`, `/system X`, `/model <substr>`, `/clear`, `/save` all emit appropriate toasts or take effect.

### Y7. Chat regenerate-from-here
- **[smoke]** Hover over an assistant turn → Regenerate. Everything after that turn vanishes; new response streams in place.

### Y8. Chat export
- **[smoke]** Header → Export → Markdown. Downloaded `.md` contains the session with system prompt + each turn.
- **[smoke]** Header → Export → JSON. Valid JSON with `messages`, `temperature`, `max_tokens`.

### Y9. System prompt quick-switch
- **[smoke]** Chat → Library dropdown. Picking a prompt populates the System field.

### Y10. Inline structured rendering
- **[standard]** Ask the model for a JSON object. Fenced ```json block renders below with a collapsible "Rendered json" section.
- **[standard]** Ask for a CSV table; the fence renders as a table view.

### Y11. Snippet library
- **[smoke]** Pin an assistant turn. /snippets page shows it, Copy works, Delete removes it.

### Y12. Multi-file tree preview
- **[standard]** Arena with a multi-file code answer. Click "Save N files…" — preview panel shows each file with checkboxes + rename fields.

### Y13. Model family grouping + deprecation
- **[smoke]** /models → Group toggle groups by family prefix.
- **[standard]** Mark one model deprecated in Notes with a replacement; card shows amber "Deprecated — use X" banner, opacity dimmed.

### Y14. Token budget meter
- **[standard]** Have a moderately-long chat. Progress bar under the system-prompt bar fills green → amber → red as usage crosses 70% / 90% of context_window.

---

## Today (2026-04-20) — new additions

### T1. Global default MLX engine
- **[smoke]** Settings → MLX Backend → "Default engine for MLX models" dropdown. Set to `mlx_lm`, save. Load a model without a per-model preference; the chosen engine is used.

### T2. HumanEval prompt side pane
- **[smoke]** HumanEval run page → click the FileText icon on any row. Side pane opens with the full problem prompt. Click again or X to close.

### T3. /planner polish
- **[smoke]** /planner → selection collapses to a summary row by default; Plan headline reads at text-xl.

### T4. Model-parse expanded tests
- **[smoke]** `cd frontend && npx tsx lib/model-parse.test.ts` → "all 112 checks passed". Tests include Llama-3.x, Phi-3.5, Q4_K_M, int4, MXFP8 cases.

### T5. Recovery: Clean start button
- **[standard]** `kill -9` the backend while a model is loaded. Restart. Banner shows three buttons: Restore, Clean start, Dismiss. Clean start wipes snapshot + download history after confirm.

### T6. Auto-bench manual trigger
- **[smoke]** `POST /api/auto-bench/trigger` with `{"model_id": "mlx:<id>"}`. Job kicks off in background.
- **[smoke]** `GET /api/auto-bench/results` lists the completed run once it's done.

### T7. Reddit watcher
- **[smoke]** Sidebar → Reddit. Disabled by default. Enable → Scan now.
- **[standard]** Scan fetches posts from r/LocalLLaMA (public endpoint, no creds needed for read). Drafts queued as pending.
- **[smoke]** Approve / Reject / Delete round-trip each flip the status; Mark posted moves approved → posted.
- **[smoke]** Config panel: each field has help text underneath explaining its purpose.
- **[smoke]** Subreddit picker: 15 curated presets render as a checkbox grid (LocalLLaMA, LocalLLM, MachineLearning, LanguageTechnology, LLMDevs, Oobabooga, ollama, huggingface, ChatGPT, OpenAI, ClaudeAI, singularity, StableDiffusion, ArtificialIntelligence, deeplearning). Checking/unchecking persists to `/api/reddit/config`.
- **[smoke]** Adding a non-preset sub via the bottom text input adds it to a separate "Custom" chip row; `r/` prefix is stripped automatically.
- **[smoke]** Config panel → "How do I get a Client ID and Client Secret?" expandable card walks through the Reddit app creation flow.
- **[standard]** Drafts: 35B Qwen3.6 (thinking) produces clean replies — no "Here's a thinking process" preamble, no meta-commentary. If preamble leaks, the server-side stripper catches it.
- **[smoke]** Approve + open on Reddit copies draft to clipboard, opens the post permalink in a new tab, and flips status to approved. After manual posting, Mark posted moves it to the Posted bucket.
- **[verified-live]** 2026-04-20 evening: 15 posts fetched across 3 subs, 15 drafts generated in one scan, every draft clean after the thinking-mode fix landed.

### T8. Gists
- **[smoke]** `curl -X POST /api/gists -d '{"title":"t","content":"hi"}'`.
- **[smoke]** `curl /api/gists/{slug}/raw` returns the markdown.

### T9. Reading-level
- **[smoke]** `POST /api/textutil/reading-level {"text": "A sample paragraph with a few sentences and basic words."}` → returns `{grade, sentences, words, ...}`.

### T10. Quantization advisor
- **[smoke]** `POST /api/quant-advisor {"param_count_billion": 30, "ram_budget_gb": 32}` → returns ranked options with `recommended` set.

### T11. Wishlist
- **[smoke]** `POST /api/wishlist` then `GET /api/wishlist` → new entry shows. `DELETE /api/wishlist/<repo>` removes it.

### T12. Load timings + predictor
- **[standard]** Load a model via /models. After completion, `GET /api/load-timings` shows an entry for that model_id with count ≥1.
- **[smoke]** `GET /api/load-timings/<id>/predict` returns a predicted_ms value.

### T13. Per-model changelog
- **[standard]** On a model with origin_repo set, `GET /api/models/<id>/changelog` → list of recent HF commits. Cached 6h.

### T14. User-uploaded evals
- **[standard]** Drop `~/.config/crucible/evals/mine.jsonl` with a few `{id, category, prompt, scorer_kind, scorer_args}` rows. Next `/eval-suite/start` includes them in `total_items`.

### T15. Cross-model eval matrix
- **[scaffold]** Run eval-suite on 2+ models. Compare by category via `/api/eval-suite/<id>` responses — UI matrix is a follow-up.

### T16. Param sweep
- **[standard]** `POST /api/param-sweep/start {"model_id": "<id>", "prompts": [{"id":"q","prompt":"What is 2+2?","expected_substring":"4"}]}`. Poll `/api/param-sweep/<id>` for cells with per-grid pass rate.

### T17. Arena share link
- **[standard]** Complete a battle. Navigate to `/arena/share/<battle_id>`. Full read-only view of both responses + winner banner.

### T18. Image input (VLM)
- **[standard]** Load a VLM (Qwen3.5-VL). `curl -F image=@photo.jpg -F prompt="describe" /api/vision/describe` → text response.

### T19. Log viewer
- **[smoke]** /logs — each source with a log file on disk appears; clicking follows via SSE.

### T20. Ops dashboard
- **[smoke]** /ops — uvicorn / next-server / omlx each show running=true with PID + RSS.
- **[smoke]** Click Restart next to a known-safe service (e.g. mlx_lm if configured) → exit code surfaces.

### T21. Usage tracker
- **[standard]** Make a few /v1/chat/completions calls. `/usage` page shows per-day totals + per-caller breakdown.

### T22. Cron workflows
- **[smoke]** `POST /api/cron-workflows {"workflow_id": "<id>", "cadence": "daily", "hour": 7, "minute": 0}` → schedule stored, appears in list.
- **[long]** Wait for the scheduled minute (or set it to 1 minute out). Workflow run fires automatically.

### T23. Notification routes
- **[smoke]** `POST /api/notification-routes {"name":"slack","event":"*","target":{"kind":"slack","url":"<webhook>"}}`. A subsequent `download.done` webhook goes to Slack.

### T24. Battery-saver schedules
- **[standard]** Add a schedule with `battery_saver: true`. Unplug the laptop; `pmset -g batt` shows "Battery Power". At the schedule's minute, the rule does not fire. Plug in → fires normally.

### T25. rsync backup
- **[smoke]** `POST /api/backup/rsync {"destination":"/tmp/crucible-backup/","dry_run":true}` → exit 0, stdout shows the file list.
- **[standard]** Same call with `dry_run: false` and a real destination (SSH or local path).

### T26. Error taxonomy
- **[smoke]** `POST /api/errors/classify {"message":"cuda out of memory"}` → category `"oom"`.

### T27. Rate limiting
- **[standard]** Burst 25 identical `/v1/chat/completions` calls rapidly. After ~20 the server returns 429.

### T28. Message edit & branch
- **[standard]** In chat, hover a past user turn → Edit & branch. Change the text and confirm. Conversation truncates to before that turn, model re-runs with the new input.

### T29. Conversation search
- **[smoke]** `GET /api/chat/search?q=<term>` → rows with session_id, role, content, title.

### T30. Git-aware prompts
- **[smoke]** `POST /api/git/context {"path": "/Users/jim/projects/crucible"}` → returns branch, status, diff, log.

### T31. Model chaining
- **[standard]** `POST /api/chain/run {"initial_input":"hello","steps":[{"model_id":"<small>","template":"Translate to French: {input}"},{"model_id":"<big>","template":"Make the previous response more formal: {input}"}]}` — sequential run, unloads between, final output returned.

---

## Known gaps / deferred

See `docs/DEFERRED.md` for items 10 / 14 / 35 / 37 / 39 / 40 that require more
scoping than a single day's pass — they ship backend scaffolding (or don't)
but no user-facing flow yet.
